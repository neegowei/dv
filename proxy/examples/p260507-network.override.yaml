networks:
  proxy:
    external: true
    name: ${PROXY_NETWORK:-shared_proxy}

services:
  web:
    networks:
      internal:
      proxy:
        aliases:
          - p260507_web

  pocketbase:
    networks:
      internal:
      proxy:
        aliases:
          - p260507_pocketbase
